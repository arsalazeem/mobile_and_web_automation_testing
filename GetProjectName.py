import requests
import json

# def get_project_name(target):
#     response = requests.get("https://portal.enabling.systems/api/app/get-projects")
#     temp = response.content
#     temp = json.loads(temp)
#     for a in temp["data"]:
#         if ["data"]["id"] == str(target):
#             p_name = ["data"]["projectName"]
#             return p_name





# response = requests.get("https://portal.enabling.systems/api/app/get-projects")
# print(response)
